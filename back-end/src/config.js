/**
 * Define a token key which is used to encode/decode JSON web tokens along with userId.
 * This key can be anything but should not be known to users. Please change this key before deploying.
 */

module.exports = {
  tokenkey: "secretpassword"
};
